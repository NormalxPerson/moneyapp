package model;

public class DebitAccount extends Account {

    public DebitAccount(String accountName, int account_ID, long balance, String type) {
        super(accountName, account_ID, balance, type);

    }
}
